#include <bits/stdc++.h>
using namespace std;
#define int long long int
int factorial(int n)
{
    return (n == 1 || n == 0) ? 1 : n + factorial(n - 1);
}
int32_t main()
{
    int n, m;
    cin >> n >> m;
    int t1 = n / m, t2 = n % m;
    int mx = (n - m) * (n - m + 1) / 2, mn = (t1 * ())
                                                 cout
                                             << minteam << " " << mxteam << endl;
    return 0;
}